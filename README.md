# burger-stack

バーガーを積み上げて得点を出す、実験段階の小さなブラウザゲーム。

## これは何か

確かめたいことは一つだけ ——「バーガーを積むこと自体が面白いか」。

デッキ（山）の中身を変えて連打し、「さっきより明らかに当たりが増えた」と
体感できるかを見るための最小構成の試作。詳しい仕様は [SHIYOU.md](./SHIYOU.md) を参照。

面白さの指標は平均点ではなく **最高点 ÷ 平均点**。これが3倍以上出れば、
点数の落差（=面白さの本体）が成立していると判断する。

## 遊び方（実装後）

1. ボタンを押すとバーガーが1個できて点数が出る。
2. プレイヤーはバーガーの中身を選べない。操作できるのは山の中身だけ。
3. 山を調整 → 連打 → 点数の落差を棒グラフで見る、を繰り返す。

## 今どこまでできているか

- [x] 仕様書（[SHIYOU.md](./SHIYOU.md)）
- [x] GitHub Pages で公開できる状態（現在は動作確認用に "Hello" とだけ表示）
- [ ] ゲーム本体（未実装）

現時点の `index.html` は Pages の疎通確認用のプレースホルダーです。
ゲーム本体はこれから実装します。

## 実装方針

HTML / CSS / JavaScript のみ。ビルド不要・依存ライブラリなし。
ブラウザでファイルを開けば動くこと。
