# burger-stack 試作0 仕様書

## 0. この試作の目的

確かめたいことは一つだけ。「バーガーを積むこと自体が面白いか」。

具体的には、次の体験が起きるかどうかを見る。

- デッキの中身を変える
- 連打する
- さっきより明らかに当たりが増えた、と体感でわかる

面白さの本体は「点数が大きく出たときの落差」だと考えている。したがって、測るべき数字は平均点ではなく **最高点 ÷ 平均点**。これが3倍以上出るなら落差は成立している。1.5倍程度なら、ルールが平坦すぎるということ。

以下は、この一点を確かめるための最小構成である。ここに書いていない要素（店舗拡大、宇宙進出、エンチャント、プレステージ、ノルマや失敗、ラン構造、メタ進行）は意図的に外している。実装しないこと。

> **【追記・2026-08-10】方針転換：ラン制（失敗あり）を採用した。** 「積むこと自体が面白いか」の判定は、現在の演出がすでに Balatro 型のギャンブル演出になっていることで前向きについたと判断し、純粋なインクリメンタル（失敗なし・無限インフレ）路線は不採用とした（経緯は KENTO.md）。これに伴い、上で「実装しない」としていた **ノルマ・失敗・ラン構造・通貨** は、**仮メカニクス**として実装した（第10節）。メタ進行はまだ実装しない。第0節・第9節の「実装しないこと」は、この4点に関しては第10節に上書きされる。

## 1. 遊び方

画面にボタンが一つ。押すと、バーガーが1個できて点数が出る。

プレイヤーはバーガーの中身を選べない。手札選択はない。落ちてくる順もランダムに決まる。

プレイヤーが操作できるのは、デッキ（山）の中身だけ。これは連打とは別の場所（画面下部）で、いつでも変更できる。

判断はランの中ではなく、ランの外にある。連打中は見ているだけでよい。

## 2. バーガーの構成

1個のバーガーは、下から順に:

| 段 | 中身 |
| --- | --- |
| 1（最下段） | 下バンズ |
| 2〜n-1 | 具材 ＋ ソース（並び順は**抽選**。下記「盤面の整列」） |
| n（最上段） | 上バンズ |

**総段数は「バンズ2 ＋ 引けた具材 ＋ 引けたソース」**で、枠を買えば増える（初期は 2＋3＋1＝6段）。

山は**デッキの2つ（具材山・ソース山）と、デッキとは別のバンズの所持枠**に分かれている。**バンズは Wave7 Step4 でデッキから切り離した**（第14節）。バンズはデッキ枚数・命中率・軸の傾斜・カードのレアリティ抽選には一切乗らない。

**抽選は非復元（2026-08-11 変更）**：**1個のバーガーを組む間、引いた札は山に戻さない。** `count`（所持枚数）が引ける回数として機能する（**2枚持てば同じバーガーに2回まで・1枚なら1回だけ**）。**同一カードが所持枚数を超えて並ぶことはない。** ラン内の次のスピンでは山は元に戻る（**1バーガー単位でのリセット**。デッキの `count` は減らない）。
- **段数（具材枠・ソース枠）は「組める段数の上限」**。**実際に組むのは引けた枚数まで**（デッキの枚数で止まる）。段を買いすぎても引ける札が無ければそこで止まり、**空段は作らない**（余った段は詰める）。空段を作らない理由：きんぴら・えび・フォアグラ・とんかつソース等の**隣接を見る効果が、空段で断絶しないようにする**ため。段の買いすぎは無駄金になるだけで、盤面の挙動は壊れない。
- **「組める段数」を常時表示（2026-08-11 追加）**：画面上部（軸内訳の下）に、いま組める段数と、山ごとの**枠の上限**と**デッキにある枚数**の両方を出す（`#tierCap`）。実際に組める段＝**枠の上限とデッキ枚数の小さい方**。デッキ枚数が枠より少なく段が制限されているときは「デッキ◯→不足」と赤く示す（例：「具材 6（枠10・デッキ6→不足）」＝枠は10だがデッキ6枚なので6段）。段数スロットを買うか、デッキを増やすかの判断に使う。
- **バンズは所持枠（4枚）からの抽選（Wave7 Step4 変更）**：デッキではなく専用の4枠に持ち、そこから **2枚**を非復元で引いて上下に置く。上下どちらに来るかも抽選。**基礎点は0**（ルールの層＝総段数には数えるが第1項には入らない）。所持枠が1枚しか無ければ下バンズだけで組む。0枚なら組めない。詳細は第14節。
  - **初期デッキからバンズは外した**（`count:0`）。ラン開始時の4枠は、つなぎのゴマバンズ4枚で埋める。

**盤面の整列（Wave7 Step0 変更・抽選）**：中段（具材・ソース）の並びは **`grand()`（シードRNG）による抽選**で決める。Wave1 の「重さ（`weight`）で決める。重いほど下」は**撤回した**（理由は KENTO「並びの重さ制を撤回」）。プレイヤーが決められるのはデッキの中身だけで、構築で確率を作り、盤面で振る。

- **抽選は `buildPlan()`（計算層）の中で1回だけ行う。** タップした時点でそのスピンの結果が全部確定し、演出は確定済みの結果を再生する。**段が落ちるたびに振ることはしない**（演出が結果を作ると再現性が壊れるため）。同一シードなら全スピンの並びと得点が完全に一致する。
- **バンズの上下も抽選する**（Wave7 Step0）。同じ2枚が出ても、どちらが上に来るかで結果が変わる。
- **もち・麩は「盤面に自分と同じ軸のカードがあれば、必ずその隣に来る」**（同軸が複数あればランダムに1枚・無ければ通常の抽選）。重さの補正（真下+100／真上−100）は撤去した。**2枚とも同じ効果**で、差別化は後回し（KENTO に申し送り）。
- **位置・隣接・連続・昇順に依存するカード（かぼちゃ・五目あん・万能だれ・福神漬け・味醂・のり・串カツ・茶碗蒸し・ウインナー・お好み焼き・かき揚げ 等）は、ルールも効果文も変えていない。** 成立の仕方が「配置」から「確率」に変わるだけで、判定処理は同じでよい。**枚数を増やせば期待値が上がる**という関係が、軸を厚くする圧として働く。
- **旧「引きの補正（orderFix・item16）」は Wave1 で撤去済み。**

## 3. 食材と初期の数値

数値はすべて仮。あとから画面上で変更できるようにすること（第6節）。

### 具材山（初期9枚）

| 名前 | 系統 | 基礎点 | 初期枚数 |
| --- | --- | --- | --- |
| パティ | 肉 | 20 | 2 |
| チーズ | 乳 | 10 | 2 |
| レタス | 野菜 | 8 | 2 |
| トマト | 野菜 | 8 | 1 |
| オニオン | 野菜 | 6 | 1 |
| ピクルス | 野菜 | 4 | 1 |

### ソース山（初期3枚）

| 名前 | 基礎点 | 初期枚数 |
| --- | --- | --- |
| ケチャップ | 2 | 1 |
| マスタード | 2 | 1 |
| マヨネーズ | 2 | 1 |

### バンズ山（初期4枚）

| 名前 | 基礎点 | 初期枚数 |
| --- | --- | --- |
| ゴマバンズ | 5 | 2 |
| イングリッシュマフィン | 5 | 1 |
| メロンパン | 12 | 1 |

バンズに特殊効果は持たせない（保留中）。現時点では基礎点のみ。ただし、あとから「バーガー全体に倍率をかける」効果を足せる形で実装しておくこと。データ構造に効果欄を用意し、今は空にしておく。

## 4. 得点のルール

**得点 =（第1項 基礎点 ＋ 第2項 育ち ＋ 第3項 加算）×（乗算の積）**　（Wave4 で3項に分けた）

| 項 | 中身 |
| --- | --- |
| 第1項 基礎点 | 印字 `base` ＋ `pb`（永続の基礎点上乗せ・第13節）。カード自身が持っている値。盤面操作（かつおぶし・飴細工・蟹カマ・あわび）はここを書き換える |
| 第2項 育ち | `g`（育ち）× `GROW_PER`（=15・**仮値**）。**出汁・醤油の修飾はこの項にだけ効く**。バンズは対象外（第12節） |
| 第3項 加算 | 効果由来の加算。**味醂（×1.6）はこの項の hit だけを見る** |

- **効果の文中の「基礎点」は第1項だけを指す。** 蟹カマ・飴細工・金箔・フォアグラ・あわびは第1項を読み書きし、**育ちを読まない**。理由：育ちは海鮮の縛りであり（KENTO「軸ごとの縛り方」）、他軸のカードが育ちを取り込めると高級が他軸の資源で得点する経路になるため。
- 乗算は各カードの効果と、`pm`（永続の倍率上乗せ・第13節）の `×(1+pm)`。**乗算が 0 になる経路がある**（フグ）。
- **内訳表示は 基礎点／育ち／加算／乗算の4欄**（＋合計）。3項の合計と表示は必ず一致する。

### 効果文で「基礎点」と書くかどうか（Wave6 で統一）

上の「効果の文中の『基礎点』は第1項だけを指す」は**効果が読む側**の決めごとだった。**表示（`EFFECT_TEXT`）の書き方**も同じ線で揃える。

| どのカードか | 書き方 | 例 |
| --- | --- | --- |
| **第1項を書き換える** | 「基礎点+N」「基礎点を〜」と書く | 豆腐・福神漬け・蟹カマ・飴細工・あわび・かつおぶし、および `pb` を伸ばすカード（熟成肉・北京ダック・搾菜 等） |
| **第3項に加算するだけ** | **「+N」とだけ書く。「基礎点」の語を使わない** | 麻婆豆腐「段数1段につき +15」／白米「上下に接する同じ軸1枚につき +45」／ちゃんぽん麺「軸の種類1つにつき +65」 |
| **第1項を読む（条件に使う）** | 読む対象の側は「基礎点」のまま | フォアグラ「上下に接するカードの基礎点を取り込み ×2.0」／金箔「上下2枚の基礎点の合計100につき +90」／お好み焼き「基礎点が下から上へ増えている組1つにつき +65」 |

- `pb`（永続の基礎点上乗せ・第13節）は第1項に入るので、`pb` を伸ばすカードは「基礎点+N（永続）」と書く。
- **負の値は符号をそのまま出す**（`sgn()`）。「+」を決め打ちで前置すると和牛サーロイン（per −15）が「+-15」になる。

**効果はすべてカードの `effect` に持つ（系統A/B の `CONFIG.rules` は Wave1 で撤去）。** 旧「系統A：ソース（ケチャップ 接する野菜×1.4 等）」「系統B：とろけチーズ・シャキシャキ」は、対象カードが95枚プールへの差し替えで消えたため廃止した。ソース＝乗算・具材＝加算という役割分担は KENTO「軸・カード・バンズの作り直し方針」に移した。

### バンズの層（Wave7 Step4・「系統」ではなく「層」）

**バンズは「系統C」ではなく、得点式の外側に掛かる独立した層である。** Wave6 までの「系統C：バンズ（全体に効く）」という書き方をやめ、**層**として書き直した（KENTO の未決に記載があったもの）。

- **バンズの倍率はバーガー全体に掛かる。** 個々のカードの得点には触らない。式の位置は次のとおり：

  **得点 =（第1項 基礎点 ＋ 第2項 育ち ＋ 第3項 加算 ＋ バンズの加算）×（カードの乗算の積）×（バンズの倍率の積）**

  バンズの加算（#4 大盛りのバンズ 等）は第3項に合流し、バンズの倍率は乗算の積に合流する。**内訳表示にはバンズの行が出る**（「バンズ ◯◯：〜 ×N」）。
- **合成の順序**：カード効果 → **バンズの層** → コンボ数ボーナス。バンズの層をカード効果より後に置く理由は、#28「加算のバンズ」がソースの出した倍率を読み、#29「見立てのバンズ」がその盤面で不発だったカードを読むため。カード効果より前では判定できない。
- **総段数の数え方**：バンズは**段としては数えるが、第1項には 0 を入れる**。バンズを含めた段数を条件にするカード（麻婆豆腐「段数×15」等）はバンズも数える一方、バンズ自身は基礎点を出さない。**盤面の中段（バンズを除く）だけを見る判定**は、バンズの層のルール側で「中段」として明示している（#3 小盛り・#4 大盛り・#7 寄せ集め 等）。
- **バンズは軸を持たない**（`cuisine:"none"`）。軸の傾斜にもカードのレアリティ抽選にも乗らない。バンズ自身のレアリティは**バンズ専用の3段（コモン／レア／極）**で、専用の抽選を使う。

## 5. 見た目

### デッキ一覧（読むだけ・Wave6 で追加）

**軸バッジ（軸 和2 中華8 … 計17）をタップで開く。** 新しいボタンは増やさない。バッジの末尾に「▸ 一覧を見る」を出して、タップできることを見せる。

- **読むだけ。どこをタップしてもカードは消えない。** 取り除きは店（有料）のまま。それまでデッキの中身を全部見られるのは店の「取り除く」を開いたときだけで、**確認のためだけには開けなかった**（1枚タップすると即座に取り除かれる）。
- **いつでも開ける**（3択の最中・店・スピン中・リザルト）。画面いちばん上に固定で重ねる別のオーバーレイなので、閉じたら元の画面がそのまま残る。閉じるボタン／外側タップ／Esc で閉じる。
- **見出しは「引かれたときの上下関係」。** デッキ全体を並べても実際に積まれるのは引かれた数枚だけなので、「積まれる順」とは書かない。
- **既定は重さ順で、軽いものが一番上**（＝上バンズ寄り）。バーガーの絵と向きが一致する。両端に「▲ 上バンズ側」「▼ 下バンズ側」を出す（通常の読み順と逆になるため）。**バンズは重さ0で整列の対象外**なので、リストの両端に固定して表示する。
- **並びはタップで4つ巡回**：
  1. **重さ順**（既定・軽い順）
  2. **点数順** … 実効値（印字 `base` ＋ `pb` ＋ 育ち×`GROW_PER`）の高い順。同名でも個体ごとに違う位置に来る
  3. **軸順** … `CUISINE_ORDER` の順。軸の中は重さ順
  4. **山順** … 具材 → ソース → バンズ。山の中は重さ順
- **各行に出すもの**：カード名／山（具材・ソース・バンズ）／軸／**基礎点**（印字と伸びぶんを分ける。例「基礎点10（+216）」。`pb` が0なら括弧を出さない）／**重さの数値**（同じ重さは引いた順で決まり、並びを見ても区別できないため）／**育N**・**×N.NN**（0のときは出さない）。**同名でも個体ごとに1行**（育ち・pb・pm が違う）。
- **もち・麩には「↕並びを変える」印を付ける。** この2枚は「そのとき何が引かれたか」で対象が変わるので、静的な一覧には表現できない。印だけ付けて効果文で読ませる。

**重さは3択と店のカードにも出す**（「基礎10・肉・重さ145」）。並びは重さで決まるのに、それまでどこにも表示がなく、「上下が〜」系（かぼちゃ・五目あん・万能だれ・福神漬け・味醂・のり）が発動するかをプレイヤーが予測できなかった。**盤面のカードには出さない**：18段の盤面では1段が約18pxまで縮み、カード名すでに重なって読めないため（実測して決めた）。

### バーガーの描き方

各食材を「横から見た1枚の絵」として描く。断面図ではない。実物のバーガーを横から見た形。レタスは横にヒラヒラがはみ出す、パティは厚みと焦げ目が見える、チーズは端が垂れる、など。

絵は画像ファイルではなく、コードで図形として描く（CSSまたはCanvas）。試作段階では色と形を即座に調整できることが重要。

段と段は食い込ませて重ねる。段の間隔を絵の高さより小さくすることで、押し潰されて挟まっている感じを出す。食い込み量は食材ごとに変えられること（レタスは柔らかいので深く潰れる、パティは硬いのであまり潰れない）。

上の段ほど手前に描画する。

### 演出

演出は役割で場所を分ける。**左＝いま何が起きたか（流れ）／右上＝どこまで積み上がったか（状態）／中央〜全体＝帯（いまどの倍率帯にいるか）**。以後、演出の変更はこの節を正とする。

**積み方**

段が上から落ちて積み上がる。順に落ちることで「当たりが来る予感」を出す。着地した瞬間、下の段が一瞬縮む（重さの表現）。総段数は可変（上下バンズ＋中段の具材・ソース。既定6段）。背が高くなっても表示部の高さは固定し、収まるよう縮小する。

**演出の時間配分の方針：時間を「期待が高まっている場所」と「報酬を受け取る場所」に寄せる。** 積み中に何度も起きる演出（帯跨ぎ）は短く軽く保ち、1個に1回しか起きない場所（完成・回収）に時間をかける。

**落下速度＝その回の期待度（最重要）**

- **計算層で1個ぶんの結果は確定しているので、落とす前からその回の最終到達帯が分かる。これを落下速度に使う。**
- **最終到達帯が低い回は落下を速く**（＝つまらない回はサッと流す）、**高い回は落下を遅く**する（段が落ちるたびに時間をかけ、期待が育つ）。**落下速度そのものが期待度の表示**であり、速い＝つまらない回・遅い＝大きい回、とプレイヤーが体で分かる状態を狙う。
- **最終到達帯ごとの落下時間を調整欄から個別に設定できる**。既定は現在の 0.3 を中程度とし、低い回は 0.15、高い回は 0.6 程度まで変える。

- **1クリックで落とす段数は調整欄で変えられる**（0＝全段〈既定〉／1〜＝その段数）。**既定は「全段」**（1クリックで1個ぶんが落ちる）。**複数段の場合でも、一気にまとめて出さず、必ず1段ずつ順に落ちる。**演出・音程の上昇・コンボ表示は1段ごとのまま維持し、変わるのは「クリックの回数」だけ。
- **段が落ちてから次の段が落ちるまでの間隔**（既定 0.12 秒）を調整欄で変えられる。この間隔があることで、複数段でも1段ずつの予感→着地→発動の単位が保たれる。
- **早送り**：落下中に**クリックする／ボタンを押しっぱなしにする**と、落ちている段の落下が速くなる（残り時間を詰める）。押しっぱなしの間は続く段も次々と早送りされる。演出・音程・コンボはそのままで、時間だけ縮める。調整欄で ON/OFF でき、**既定は ON**。
- 落下中の追加クリックの扱いは従来どおり「次段を予約／無視」を切り替えられる（予約時はさらに段を要求、無視時は要求を増やさない）。

**文字は3種類だけ（最優先の原則）**

- **画面に出る文字（読解を要求する要素）は、次の3種類だけにする。** 文字は読ませる分だけ視線を止めるので、量を増やしても密度の担い手にならず、うるさくなるだけだと分かった（以前の「数字を20個以上飛ばす」方針は誤りとして撤回する）。
  1. **段ごとの基礎点**（飛ぶ数字）。段を置くたびに、着地点から小さく短命な「**+N**」を一つだけ上へ飛ばす。**飛ぶ数字はこれだけ**にする。
  2. **右上の累計倍率**「**×N.NN**」（常時表示・状態）。乗算の積はここに集約する。
  3. **点数をバーガーに重ねて大きく表示**（下記「得点をバーガーに重ねる」）。積み中は基礎点の累計、完成時は合計点。
- したがって、**倍率「×N」を各所に飛ばさない**（接触面から湧く倍率、コンボの飛ぶ数字、完成の内訳を別々に飛ばす、等はすべて廃止）。倍率は右上の累計に一本化する。
- 例外は予告のみ（下記の予告文）。これは飛ばず、その場に留まる。

**得点をバーガーに重ねる（大表示）**

- **プレイヤーが注目しているのはバーガーなので、点数はバーガーに重なる位置に大きく出す。** フィードバックは視線のある場所で起こす。
- **積んでいる最中は基礎点の累計**を、**完成時は合計点**を、どちらもバーガーの上に被せて大きく表示する。**完成時の合計は、バーガーが一部隠れるくらい大きくてよい**（読めればよく、遠慮しない）。
- **左上の「基礎 N」計器は廃止**する（バーガー下の得点内訳の枠で足りる）。基礎点の場所はこのバーガー上の大表示に移す。
- **大きさ・不透明度・表示位置（バーガー内の縦横）は調整欄から変えられる**こと。
- この大表示は**揺れの外に置き、揺れても動かさない**（激しい揺れの最中でも読める）。

**コンボ発動の伝え方（文字ではなく演出で）**

- コンボが発動したら、**接触面が光り**、パーティクル・ヒットストップ・カメラの寄り・音でその強さを伝える。**倍率の数字は飛ばさない**（右上の累計に反映される）。等級が高いほど光・破片・揺れ・音を強くする。
- **コンボ履歴（「名前 ×N」の羅列）は廃止**する。履歴という読み物を右上に積まない。

**右上：状態（累計）**

- 乗算の積の現在値「**×N.NN**」を右上に常時表示（積むほど増える）。倍率はすべてここに集まる。基礎点はバーガー上の大表示に出す（左上計器は廃止）。

**帯（band）：倍率帯の持続演出**

- 乗算の積が**等比のしきい値**（開始値 × 比の累乗。既定 ×3 から ×1.6 ずつ）を上がるごとに「帯」が一段上がる。しきい値は手で列挙せず自動生成する（終盤で尽きないように）。
- **帯にいる間はその見た目がずっと持続する**（Balatro のように、次の帯に上がると一段変わる）。持続対象：**倍率表示の色／倍率表示の大きさ／背景の明るさ／段が落ちるときの揺れ／バーガーのふちの光り**。各チャンネルは調整欄で個別に ON/OFF できる。
- 見た目は**数段階を循環**させ、**一巡ごとに全体が一段派手**になる。
- 帯を**跨いだ瞬間だけ**、中央にバーストを一瞬出す（状態の持続演出とは別）。**バーストは文字（「×N!」等）を使わず、広がる衝撃波（shockwave）の輪・発光・パーティクル・揺れ・音で伝える。** 帯が上がったことは演出の強さそのもので示し、読ませない。
- 帯が上がる瞬間は、**バーストの直前に短い無音の溜め（既定 0.2 秒）を置いてから爆発**させる。溜めは間を作り、跨いだ実感を強める。溜めの長さは調整欄で変えられる（0 で無し）。
- **2 段以上を一度に跨いだ場合は、一段ずつ順に確定**する（全部まとめて一気に出さない）。溜め → バースト → 次の段、を繰り返して各帯を個別に見せる。
- **帯が上がるたびに毎回演出を出し、帯が高いほど大げさになる。ただし強度は離散段でなく連続値で決める**（下記「連続で滑らかに上げる」を参照）。跨いだ瞬間のバーストも、その時点の連続レベルに応じて滑らかに大きくなる。イージングは**「速く出てゆっくり消える」**を既定にする。強度カーブ・段階数は調整欄で変えられる。
- **積み中の帯跨ぎは「短く・軽く」保つ（1個で複数回起きるため）。落下は止めない。** 跨ぎのバーストは短い持続・小さめの揺れ・少数の破片・弱いカメラ寄りに留め、**ヒットストップ（画面停止）は積み中には掛けず、完成に集約する**。重い盛り上がりは完成の瞬間に寄せる。持続の上限などは調整欄で変えられる。
- 音・光・揺れの強さは調整欄で個別に調整できる（**方針は「色の派手さでなく、揺れ・光・音の量と持続で押す」。既定を控えめにせず、最大は桁違いまで取れる**）。
- **揺れは最優先で強化する。** 振幅の最大値を従来の 2〜3 倍まで設定できるようにし（帯アップの大揺れの上限を調整欄で引き上げられる）、**振幅だけでなく持続時間と減衰の形（settle＝なめらか／hard＝一撃で急減衰／long＝長く尾を引く）も選べる**ようにする。
- **揺れの適用範囲はバーガーの表示（本体）だけに限定する。** バーガー表示部の枠・背景・右上の倍率計器・バーガー上の得点表示、そして画面下の固定バー（連打ボタン）や調整欄・その他 UI には**一切揺れをかけない**。揺れは専用の内側レイヤーにだけ適用し、**振幅を最大まで上げても連打ボタンの座標は 1px も動かない**こと。
- **原因の根絶：バーガー表示部（箱）は `overflow:hidden` で内容を必ず箱の中に閉じ込める。** 揺れ・破片・衝撃波が箱の外へはみ出すとページ全体が広がり、画面下の固定バー（`position:fixed`）を巻き込んで吹き飛ばす事故が起きる。箱でクリップすれば、揺れをどれだけ強くしても外へ波及しない。**揺れ以外の transform（カメラの寄り＝内側スケール、ヒットストップ、帯の見た目）も、対象をバーガー内の要素に限り、固定バーの祖先には transform/filter をかけない**（祖先に transform があると固定要素の基準がずれて動くため）。
- 振幅はバーガーが箱に収まる範囲を既定にし、係数（gain）と上限（ampMax）で調整する。**「強さ」は振幅の大きさより、持続と減衰の形・音・光で出す**（箱から飛び出すほどの振幅は、揺れではなく射出になり見た目も壊れる）。

**予告（リーチ）：本当に次の帯に届く回だけの緊張演出**

- 残り段数が少なく（既定 1〜2 段）、その回の**確定した結果が実際に次の帯を跨ぐ**とき、中央下に「**あと ×N で ×M帯**」と予告を出す。
- **外れる回に当たりそうな演出は出さない。** 予告は結果と乖離させない。クリック時点で結果は確定しているので（下記「計算層と演出層の分離」）、予告は「本当に届く回」に限って立ち、必ず帯バーストで解決する。届かないのに煽ることはない（＝「止まった」表示は出さない）。
- 予告が出ている間は、**画面全体が緊張した見た目**になる（枠が明滅し、短い緊張音が鳴る）。届いた瞬間に帯バーストへ移り、予告を消す。
- 対象とする残り段数・届く見込みの幅・ON/OFF は調整欄で変えられる。予告の一時演出（明滅）もバースト同様、次のバーガー開始時に DOM/クラスを直接片付ける。

**完成の区切りと回収**

- 段の数だけ押すと**完成して停止**する。合計点を大きく強調表示。ここで予約された入力は破棄する。
- **完成の演出は、そのバーガーで到達した最高の帯レベルに応じて変える。** 高い帯まで届いた回ほど派手にし、最上段まで到達した回は特別な演出を出す。**ここは1個に1回しか起きないので、重い演出（ヒットストップ・大きい揺れ・衝撃波・破片の連発）を集約し、長めに置いてよい。長さ（完成演出の持続）を調整欄から変えられる。** 強さも調整欄で変えられる。
- 完成直後の短時間は**入力を受け付けない**（連打中でも点数を必ず見せる）。
- **次の1回で回収**：点を統計・棒グラフに反映し、セッション累計へ**コインの着弾に同期して加算する演出**を出してから、次のバーガーが始まる。

**点数＝バーガーの価格（売値）**

- **1個のバーガーの得点は、そのバーガーの価格（売値）である。表示もそのように扱い、単位（¥）を付けて価格として読めるようにする。** バーガーに重ねる大きな数字は「¥N」の価格、得点欄は「このバーガーの売値（価格）」、累計は「売上」、統計は「最高売値／平均売値」。回収はその売値をお金として受け取る動作。

**回収の演出（売上をコインで受け取る＝コインの着弾と数字の増加を同期）**

- **回収前は、下の得点欄（「このバーガーの売値」）に価格を表示しない（空にしておく）。** 完成時に価格を大きく見せるのはバーガーに重ねた表示だけで、下の枠は回収するまで伏せる。
- **回収の瞬間、コインが周囲から集まって「収束先」へ飛ぶ。既定の収束先は得点欄（このバーガーの売値の枠）。** 飛ばすのは**コインのみ**。積もらせず、到達したら消える。飛行時間は短め。
- **コインの着弾に合わせて数字が段階的に増える（同期）。1枚が得点欄に到着するたびに、そこの数字が増えていき、全部のコインが到着した時点で最終的な価格になる。** 数字は**時間で勝手に数え上げず、着弾が数字の増加の原因**になる形にする（各コインが総額の 1/枚数ぶんを運び、合計はちょうど総額）。
- **着弾ごとに音が鳴り（コイン音）、数字が小さく跳ねる。**
- **売上の累計も同様に、コインの着弾に合わせて増える。**
- **収束先は調整欄から変えられる：得点欄（既定）／バーガー上の価格の数字／売上累計。** 湧く範囲（spread）も変えられる。
- **得点（価格）が大きいほどコインの枚数が増える**（per 点で1枚・min/max で下限上限）。＝着弾回数＝数字が増える回数＝鳴る回数。
- **回収は非ブロッキング：コインが飛んでいる最中も操作を受け付け、次のバーガーを始められる。次のバーガーが始まってもコインは飛び続け、数字（特に売上累計）は増え続けてよい。** 実装上、コインのタイマーはバーガー切替のタイマー消去に巻き込まれない専用プールで持ち、飛行は世代に縛られない。得点欄（このバーガーの売値）は次バーガーの1段目着地でそのバーガー用に空へ戻し、それ以降は古いコインが書かない（売上累計は増え続ける）。
- **コインの音は点数に比例した「延々と鳴り続ける」ものにはしない**：鳴る回数＝コインの枚数（下限〜上限でクランプ）に収め、音程は枚が進むほど上がる（`pitchSpan`）。点数の大きさは音程・厚みでも表す。
- コインは画面全体をまたぐので固定配置の専用レイヤーで出し、レイアウトは動かさない（`reduced-motion` では飛ばさず、音だけ）。
- **回収の瞬間、バーガーに重ねていた価格表示は消す**（数字がコインに変わって飛ぶ＝次の場面〈3択など〉へ持ち越さない）。

**リセットの整理（完成時／回収時に何が消え、何が残るか）**

表示を「**1個ぶん（バーガーごと）**」と「**セッション（バーガーをまたぐ）**」に分ける。

- **1個ぶんの表示**：バーガーの絵、飛ぶ基礎点 +N、乗算の積 ×N.NN、バーガーに重ねた大きな価格（積み中＝基礎点／完成＝売値 ¥N）、帯の見た目（色・大きさ・背景・ふち・揺れ）と帯レベル、帯のバースト（衝撃波の輪）・メーターの弾け・揺れ、予告（リーチ）表示と緊張の見た目。
- **セッションの表示**：売上累計、統計（最高売値／平均売値／最高÷平均）、直近20回の棒グラフ、デッキ（山の枚数）、完成回数。

- **完成時（停止）**：売値（¥N）を**バーガーに重ねて**大きく出す。**下の得点欄（このバーガーの売値）は空のまま**（回収まで価格を出さない）。「1個ぶんの表示」（帯・×累計）は残す。売上にはまだ入れない。
- **回収時（次のタップ）**：明細（基礎点/加算/乗算/合計）を出し、**得点欄と売上累計をコインの着弾に同期して 0 から段階的に満たす**（時間で数え上げない）。**バーガーに重ねた価格は消す**（コインに変わって飛ぶ）。得点欄は次バーガーの1段目着地でそのバーガー用に空へ戻す（古いコインは以後書かない）。売上累計はセッションの表示なのでコインの着弾で増え続ける。
- 帯のバーストなどの**一時演出は、その除去を演出タイマー任せにしない**。バーガー開始時に DOM/クラスを直接片付ける（次バーガー開始で演出タイマーが取り消されても、消え残らないようにするため）。

（第1層の3択を挟む設定では、回収の直後に3択を出す。**3択が出た時点で、それまでの落下の予約は破棄する**（選択後にクリックしていないのに次のバーガーが勝手に始まらないようにするため）。カードを選ぶ／スキップすると待機状態に戻り、「1個ぶんの表示」はリセットされる。**次のバーガーは、次にボタンを押したときに始まる**（選択そのものでは落とさない）。）

**音（効果音）**

- **Web Audio API でコード生成する。音声ファイルは使わない**（素材の権利を持ち込まない）。
- 着地するたびに音が鳴り、段が積み上がるにつれて**音程が上がる**。コンボは別の音で、**等級によって変える**。帯が上がると**音色そのものが変わる**。予告中は緊張音を鳴らす。
- **【全音共通の原則・最重要】このゲームで鳴るすべての音は、その時点の到達音程を基準に鳴らす。固定の高さで鳴る音を作らない。新しい音を追加するときも必ずこの原則に従う。** 「いま到達している音程」＝着地音の音階の、今の段の高さの度数（`reachedFreq`）。着地・コンボ・帯アップ・回収（コイン）・完成・緊張音、すべてこの基準の上に積む。固定の絶対 Hz や、スキーム固有の固定 root で旋律を鳴らさない（そこだけ音が浮き、直前の高い着地音から急に下がって流れが途切れるため）。
- **コンボの音は固定にしない：到達音程の上で、連鎖するたびに音程を上げ**、連鎖の長さが耳で分かるようにする（1連鎖ごとに半音ずつなど。連鎖数はバーガー開始でリセット）。
- **帯アップの音は、到達音程の1オクターブ上から層を重ねて押し寄せる**（固定 root で鳴らさない＝下がって聞こえない）。
- **回収（コイン）の音も、到達音程の上から鳴らす**（固定の低いコイン音にしない）。連鎖で少しずつ上げる。
- **完成音は、到達した高さから続けて鳴らし、帯レベルでも音色が変わる**（着地の続きの高さから和音を重ねる）。
- **帯が上がる瞬間・完成では音を層にして押し寄せる。** 同時発音数の上限を引き上げ、帯レベルが高いほど層を増やす（本体＋オクターブ上＋下支え〈到達音程のオクターブ下〉＋アルペジオ＋ノイズの当たり）。帯が上がるほど音数が増え、押し寄せる感じを出す。
- **低音（「ドン」）は廃止する。** 音程を到達音程に合わせても浮いて聞こえたため、低音そのものを無くす（関連する調整欄＝量・歪みも外す）。**重さは、音量・減衰（長めのフェード）・音の層数（下支えの三角波オクターブ下を厚めに）で出す**。
- **音のパターン（scheme）を 10 種類**用意し、調整欄で切り替えて比較できる。名前付き（標準（現行）／木琴／電子音／和風／レジ音／ベル／マリンバ／8ビット／パーカッション／パイプ）。**既定は「標準（現行）」**。切り替えても役割は共通で、音色と旋律だけが変わる。
- ON/OFF と音量と音のパターンを調整欄に置く。**既定は ON**（毎回 ON にする手間をなくす）。最初のタップ（ユーザー操作）で解錠する。

**計算層と演出層の分離**

- **クリックした時点で、その1個の結果を全部確定させる。**6段の中身・発動するコンボ・段ごとの倍率と帯・最終得点までを、一つの「確定した結果（plan）」として先に計算する。
- **演出は、その確定済みの結果を再生するだけ**にする。演出の途中で結果を計算したり、値を積み増したりしない。表示に出る乗算の積・帯・予告・合計点は、すべて plan から読む。
- ねらいは三つ。**(1) 演出をスキップして結果だけ出せる**こと。**(2) 将来の自動化・レーンの複数化に備える**こと（結果の生成と見せ方を切り離しておく）。**(3) 演出タイマーが状態を持つ構造をなくす**こと。帯レベルなどの「いまどこまで進んだか」は plan と再生位置から一意に決まり、タイマーが書き換えて持ち回ることはない。だから途中で中断（スキップ・次バーガー）しても、`clearTimers()` ＋ 一時 DOM の直接片付け ＋ plan からの再適用で、必ず正しい状態に戻せる（＝消え残りが起きない）。
- **スキップ**：演出（再生）を飛ばして、確定済みの最終状態だけを即表示する。計算層は同じなので**結果は一切変わらない**。ボタン（画面下の「⏭ 結果」）と、常に省略する設定（調整欄「演出を省略（結果だけ表示）」・既定 OFF）の両方を置く。

**演出の基本要素（juice）と、帯レベルによるスケーリング**

適切な値を触って探すための土台として、基本要素をひと通り用意し、**それぞれ調整欄から個別に強さを変えられる**。

- 用意する要素：**揺れ・発光・色・大きさ・溜め（既存）**に加えて、**パーティクル**（着地／コンボ／完成で破片が飛ぶ。ゴマ・パン粉・肉汁・湯気・チーズ・野菜など食材に応じた見た目。数・速さ・寿命・重力・広がりを指定）、**ヒットストップ**（大コンボ／帯上昇の瞬間に画面を短時間止める。停止時間を指定）、**残像トレイル**（落下段が尾を引く）、**カメラの寄り**（大発動で画面全体が少し寄る）、**数字自体の演出**（得点の文字が跳ねる・大きくなる・色が変わる・回転する）、**イージング**（落下と数字の動きに跳ね／オーバーシュートを選べる）。
- 各要素は調整欄で **ON/OFF・base（何も起きていない＝帯0のときの強さ）・scale（強度カーブの効き）** を設定できる。実効値 = base × scale^（整形済みの連続レベル）。
- **スケーリングの基準は帯レベル**とする。得点や乗算の絶対値ではなく帯に紐づけることで、**数値が青天井に伸びても演出が破綻しない**。各値には上限クランプを置く。
- **強度は離散的な段階ではなく連続値で動かす（急な段差を作らない）。**
  - 帯レベルの整数値だけでなく、**次の帯までの進捗（0.0〜1.0）**も使い、段階の「間」を補間する。
  - **1個のバーガーの最終到達帯は計算層で確定している。**これを利用し、**段を積んでいる最中から最終帯へ向けて強度を徐々に上げる**（跨いだ瞬間だけ上げるのをやめる）。持続の見た目（色・大きさ・背景・ふち・落下揺れ）は段を置くたびに連続レベルで更新する。
  - **スケーリングの曲線を調整欄から選べる：線形／指数／対数／S字**（既定は対数）。指数だけでは上位で急激になりすぎるため、対数・S字で上位の急変を抑えられるようにする。両端は不変で、途中の上がり方だけが変わる。
  - **段階数も調整欄から増やせる**（既定5。10段階以上も試せる）。
- **上位での飽和を、上限を上げずに解消する（配分を移す）。** 帯が上がっても、揺れ・光・パーティクルは上限に達すると横ばいになる。上限をさらに上げるのではなく、**飽和したチャンネルのあふれ分を、上限のないチャンネルへ順に逃がす**：揺れが上限なら**音の層数と持続時間**へ、それも頭打ちなら**画面全体の変化**（閃光の強さ・持続など）へ。これにより、視覚が頭打ちでも上位ほど盛り上がりが続く。
- **デバッグ**で帯レベルを人為的に強制でき、各要素を最大まで上げても（`overflow:hidden` の箱に閉じ込めるので）レイアウトが破綻しないことを確認できる。
- **デバッグ：点数を段階的に上げるモード。** 実際にプレイせずに、低い点数から極端に高い点数まで順に演出を再生して確認できる（各段の帯アップ→最後に完成演出）。**上がる速さ（間隔）と刻み（ステップ）、到達帯を指定できる**。上位で飽和せず盛り上がりが続くかの確認に使う。
- **方針：色の派手さでなく「揺れ・光・音の量と持続」で押す。既定を控えめにする従来方針は改め、最大値をこれまでより桁違いに大きく取れるようにする。** 派手さは調整欄で上げて探す。
- **密度は文字以外で確保する**：文字を3種類に絞ったぶん、密度はパーティクル・光・揺れ・音の量と持続で担う。パーティクルの総数上限を桁で引き上げる（〜2000 まで設定可能）。数え上げ中も破片・光・音を出し続けて無演出の時間を作らない。帯上昇は 1 箇所でなく複数箇所で同時に起こす。音は連続レベルに応じて層を増やして押し寄せる。**飛ぶ数字は段の基礎点だけ**に保ち、密度を数字の量で作らない。（破片・衝撃波はバーガーの箱の中に収まる＝はみ出してレイアウトを壊さない。）

**共通の原則**

- **演出は一切レイアウトを動かさない。連打ボタンの位置は常に固定。** バッジ・発光・倍率・帯・パーティクル・カメラの寄り・数字の演出はすべて**絶対配置の重ね／トランスフォームのみ**で出し、周囲の要素を押し下げない。バーガー表示部・得点表示部の高さは固定。カメラの寄りはバーガー表示の内側スケール（fit とは別チャンネル）だけを動かし、連打ボタン（画面下に固定）や他要素の位置は動かさない。
- `prefers-reduced-motion` に対応し、指定時はアニメーション（揺れ・フラッシュ・数え上がり・**パーティクル・ヒットストップ・トレイル・カメラの寄り・数字の跳ね**等）を省く。帯の色・大きさなど「状態」は残してよい。

## 6. 画面

一つの画面に、上から順に:

1. バーガー表示（積み上がる様子と、発動したルール）
2. 今回の得点と、その内訳（基礎点／加算／乗算がそれぞれ見える形）
3. 連打ボタン
4. 直近20回の点数を横に並べた棒グラフ。落差は1回ずつ見てもわからない。並べて初めてわかるので、これは必須。最高点・平均点・最高÷平均を数値で常時表示すること。
5. 3つの山の中身。食材ごとの枚数が見え、その場で＋−で増減できる。
6. 数値の調整欄（折りたたみ可）。基礎点、各倍率、加算値、食い込み量、演出の速さを、すべてその場で変更できる。再読み込み不要。

6番は必須。調整こそがこの試作の本体であり、数値を変えるたびにコードを書き換えるのでは目的を果たせない。

**調整欄の分類（追記）**：欄が大きくなりすぎたため、分類して折りたたむ。既定はすべて閉じた状態にする。分類は **進行（ラン・家賃・店・3択）／盤面（段数・傾斜）／カードの数値（基礎点・各カードの効果）／演出・見た目（落下速度・帯・パーティクル・揺れ・カメラ・数字）／音／デバッグ操作（帯レベル強制・点数ランプ・演出を試す）** の6つ。各分類は見出しをタップで開閉できる。

## 7. 実装の方針

HTML / CSS / JavaScript のみ。ビルド不要、依存ライブラリなし。ブラウザでファイルを開けば動くこと。

数値（基礎点・倍率・枚数・演出の速さ）は1箇所にまとめること。設定オブジェクトかJSONとして切り出し、ロジックと混ぜない。作者が数値だけを触れる状態を保つ。

ルールの定義もデータとして書く。条件をコードに直書きせず、ルールを1件足すのが1行の追加で済む形にする。あとでルールを大量に増やす前提がある。

保存機能は不要。リロードで初期化してよい。

スマートフォンのブラウザでも遊べること。ただし調整用の画面はデスクトップ優先でよい。

**ゲーム乱数のシード管理（2026-08-11 追記）**：ランの乱数をシードから再現できるようにする（外れ値のランを測定側で再生し経路を追うため。プレイログ記録の前段）。
- **対象はゲームロジックの乱数のみ**（カード抽選・shuffle・3択の抽出・キャビアの削除対象・並び補正）。これらは**シード付きの単一生成器**（mulberry32）を通す `grand()` に集約する。
- **演出の乱数は据え置き**（パーティクル・効果音のノイズ・揺れ・コインの軌道のばらつき等は `Math.random` のまま＝毎回異なってよい）。
- **ラン開始時にシードを確定**し、そのランの間は同じ生成器を使い続ける。**同じシードなら抽選順が完全に再現**される。
- **シードは外から指定できる口を用意**する（測定スクリプトからの再生用）。指定が無ければ自動生成し、記録・再利用できる形で保持する。
- 得点計算・効果判定・経済ロジックは変更しない（乱数の**取得元の差し替えのみ**）。

**プレイログ記録（2026-08-11 追記）**：手プレイのランを記録し、あとで測定側と突き合わせられる形で書き出す（外れ値のランを人手で再現・分析するため。シード管理の続き）。
- **記録の単位はランごと**。**1スピン記録するたびに localStorage を更新**し、ラン終了を待たずに保存する（途中でリロード・タブを閉じる・放置しても、そこまでの明細が残る）。保存コストは1回あたり 1ms 未満（100スピンでも約0.3ms）でテンポに影響しない。
- **確定のタイミングは2つ**：①ラン終了（家賃を払えず終了）、②明示的な新規開始（「最初から」／履歴クリア／設定リセット）。確定したランは `status:"ended"` になる。
- **ブート（リロード）時は前回の未確定ランを継続する**（別ランに分けない）。ただしゲーム状態自体はリロードで初期化されるため、**継続後のスピン番号・デッキ・営業日は振り出しに戻り、同一ランの明細配列にそのまま積み増す**（累計 `cum` だけは引き継ぐ）。一覧では「進行中・再開」と表示する。
- **既定でオン**。画面下部「🧾 プレイログ（記録）」のトグルで切れるが、**切っても既存の記録は消さない**。
- **保存先は localStorage**（確定済み `bstack.plog.runs.v1` / 進行中 `bstack.plog.current.v1` / トグル状態 `bstack.plog.enabled.v1`）。**全ランを貯める**。**容量上限に達したら古い確定ランから消す**が、消す前に**必ず画面上部の警告欄に何件消したかを表示**する（黙って消さない。進行中のランは優先して守る）。
- **1ラン ＝ 要約 ＋ 明細の配列**。JSON。
  - 要約 `summary`：`date`（ISO）、`seed`、`spins`、`finalCum`（累計得点）、`mainAxis`（最多軸）、`finalTiers`、`daysSurvived`、`finalDeck{total,byAxis}`、`source:"play"`、`status`（`"playing"`/`"ended"`）、（再開ランのみ）`resumed:true`、`bestBurger`（**最高得点のバーガー1本のみ**：`{spin, score, stack}`。`stack`＝下から順のカード名配列。リザルトの再現に使う）。
    - **`bestBurger` はそのランで最高得点のスピン1本だけを保持**する（全スピン分の `stack` は持たない＝段73のランで容量が跳ねるのを避ける）。更新方式：そのスピンの得点が既存の最高を超えたら差し替える。測定側（`source:"sim"`）でも同形式で吐く。
  - 明細 `spins[]`（1スピン1行）：`spin`（番号）、`score`（そのバーガーの得点）、`cum`（累計）、`tiers`（段数）、`deck{total,byAxis}`（デッキ枚数と軸内訳）、`artifacts[]`（所持アーティファクト）、`rent{amount,residual}|null`（家賃額と支払い後残額）、`bought[]{kind,name,cost}`（そのスピンで買ったもの）、`removed[]{name,cause}`（そのスピンで消えたカード）。
  - **削除は購入除去だけでなく効果除去も含める**：`cause` は `"shop"`（店で取り除く）／`"caviar"`（キャビアの無作為除去）／`"katsuobushi"`（かつおぶしの自己消費）。
- **書き出しはクリップボードへの JSON コピー**。①一覧から選んだラン（確定済み＋進行中）をコピー、②**「いま遊んでいるランをコピー」ボタンで進行中のランを一覧を経由せず直接コピー**（遊び終えた直後にその場で退避できる）。**絞り込み機能は入れない**。
- **一覧には進行中のランも出す**（末尾）。終了済み（緑「終了」）／進行中（黄「進行中」、再開時は「進行中・再開」）をバッジで区別する。
- **明細の JSON 構造は、将来 `tools/` の測定スクリプトからも同じ形式で吐ける**ように設計する（`source` で手プレイ／測定を区別）。
- 記録は**読み取りのみ**：得点計算・抽選・経済・乱数ロジックは変更しない。これは**測定用の記録**であり、第9節でいう「ラン状態のセーブ（リロードで途中からゲームを再開）」ではない（ゲームがリロードで初期化される挙動は変えない。継続するのは**記録**だけ）。

## 8. 最初にやること

1. **GitHub Pages を有効にする。** リポジトリ burger-stack の設定で、main ブランチのルートを公開する。公開URLを報告すること。（作者はスマートフォンから確認したい。ファイルが読めるだけでは不足で、ブラウザで遊べる状態が必要。）
2. README を書く。何のゲームか、どう遊ぶか、今どこまでできているか。
3. 第0節の目的を満たす最小構成を実装する。

## 9. 実装しないこと（再掲）

店舗拡大、宇宙進出、エンチャント、ガチャ、プレステージ、通貨、ノルマや失敗、ラン構造、メタ進行、セーブ、音、画像素材の使用。

これらは「積むこと自体が面白いか」の判定がついてから検討する。先に足すと、面白くなかったときに原因を切り分けられなくなる。

> **【追記・2026-08-10】** 判定はついたものとし、**通貨・ノルマ・失敗・ラン構造**は第10節の仮メカニクスとして実装済み。音も実装済み（第5節）。**まだ実装しないのは：メタ進行（ラン終了後に残るもの）、カードの複製、常駐の店員、店舗拡大・宇宙進出・エンチャント・ガチャ・プレステージ・セーブ・画像素材。**

## 10. ラン制の仮メカニクス（試作）

第0節の方針転換に伴う、ラン制の**骨格**。**数値はすべて仮**で、調整欄（進行カテゴリ）から変更できる。目的は「仮の数値で何営業日まで生き残れるか」を測れる状態にすること。メタ進行はまだ入れない。

### スピンと営業日

- **バーガー1個 ＝ 1スピン。** 完成→回収で1スピンを消費する。
- 画面には現在の**営業日**・次の家賃・次の家賃まで残りスピン数を表示する（`#runStatus`）。

### 家賃（ノルマ）

- **一定スピンごと（既定5スピン）に家賃の支払いが発生する。** 間隔は調整欄で変えられる。
- **家賃は等比で上がる。** その回の家賃 = 初期値 × 倍率^（これまで支払った回数）。初期値（既定 500）と倍率（既定 ×1.5）は調整欄で変えられる。
- **【追記・Wave 11】家賃は営業日ごとに個別に置く（`run.rentByDay`）。** その営業日の値が入っていればその額、空欄なら上の等比の式に戻る。既定は等比の式と同じ額（300・420・588・823・1152・1613・2259・3162・4427・6198）を10営業日ぶん入れてあるので、置き換えの前後で挙動は変わらない。配列より先の営業日（既定では11営業日目以降）は等比の式のまま。**調整欄「ラン制：営業日ごとの家賃」から営業日ごとに変えられる**（「等比の式から作り直す」で `dayGoal` の日数ぶん作り直せる）。
- 家賃が来たら支払い画面を出す。**所持金から家賃を引く。払えれば営業日が1つ進み**、続いて店が開く。**払えなければランが終了する**（下記）。

### 通貨は1本（所持金）

- **バーガーの売値がそのまま所持金になる。** 回収したコインは所持金に積まれる（表示ラベルは「所持金 ¥」）。売上の累計という別勘定は持たない＝通貨は所持金1本。
- 家賃を払うと所持金が減る。**残りが買い物に使える。**
- 実装上、家賃は「今この瞬間の所持金」で判定する必要があるため、所持金は**論理値（即時）と表示値（コイン着弾で追いつく）**の二つを持ち、表示値は論理値を超えない。家賃の判定・支払いは論理値で行う。

### 店（家賃の支払い後に開く）

家賃を払うと店が開き、**残った所持金で買い物**をする。品揃え（買えるもの）：

- **デッキにカードを1枚加える**（品揃えの中からカードを選んで購入。既定 ¥100）。
- **デッキからカードを1枚取り除く**（既定 ¥75）。**※以前スキップで無料でできていた「1枚取り除く」は廃止し、削除は店でのみ・有料で行う。**
- **具材の枠を1つ増やす**（1個のバーガーに載る具材の枚数を+1。既定 ¥150）。
- **ソースの枠を1つ増やす**（同、ソース枠を+1。既定 ¥150）。
- **品揃えの引き直し**（店に並ぶカードを引き直す。既定 ¥50）。
- 値段はすべて調整欄で設定できる。**購入回数に応じた値上がり（scaleOnBuy と倍率 scaleMul）は要素ごとに独立**：**具材枠・ソース枠・カード・引き直し・アーティファクト・取り除き**の6要素に分けて、それぞれ ON/OFF と倍率を持つ（`shop.scale.{slot,sslot,card,reroll,artifact,remove}`。一律スケールは廃止）。
  - **具材枠とソース枠は別要素（2026-08-11 分離）**：`slot`＝具材枠・`sslot`＝ソース枠。カード寄与測定（#63）で「単体では負だが揃うと百万規模」のカードがほぼソースに集中していた（倍率＝乗算がソースに寄っている）ため、倍率を増やす操作＝ソース枠は高くつくべき、という判断で分離した。
  - **現在の値（試験値・遊んだ感触で変えうる）**：具材枠 ON×2.0（150→300→600→…）、ソース枠 ON×3.0（150→450→1350→…）、引き直し ON×2.0、取り除き ON×2.0、**カード・アーティファクトは既定のまま（OFF）**。購入回数は要素ごとに別カウント（一方を買っても他方の価格は上がらない）。
- 所持金が足りない品は選べない（灰色表示）。**「店を出る」**で買い物を終え、次のスピンへ戻る。

#### ソース枠だけ上限つき・固定価格（Wave6）

- **ソース枠は3枚まで**（`shop.sauceSlotMax: 3`・調整欄から変更可）。**ソースはほぼ全てが乗算カードなので、ソース枠は乗算の枚数を直接増やす**＝得点が積で伸びる。等比の値上げでは追いつかない（実プレイで枠5枚まで買われ、得点が 2,984 → 45,569 に跳ねてランが壊れた）。価格ではなく**上限**で止める。
- 上限に達したら店のボタンを**無効表示にして理由を出す**（「ソース枠は3枚まで」）。**ボタンは消さない**（買えないことが見えるほうがよい）。
- **上限3・初期1なので購入は生涯2回だけ。** 等比の式をやめ、**固定価格の配列** `shop.sauceSlotPrices: [2000, 12000]`（1回目・2回目。**どちらも仮値**）にした。上限を手で上げたときは配列の最後の値段を使い回す（買えなくならないように）。
- **具材枠には上限を設けない。** ×2 のスケール（150→300→600→…）のまま。具材は加算寄りで、枠を増やしても積では伸びない。
- `shop.sauceSlotCost` と `shop.scale.sslot` は**もう使っていない**（後方互換で残置。調整欄の見出しに「未使用」と書いてある）。

### 毎回の3択（無料のまま）

- **スピンごとのカード選択（3択）は無料で維持する。** 頻度は調整欄（既定は毎スピン）。
- **スキップは「見送る（何もしない）」に変更**。以前の「1枚取り除く」は廃止（削除は店のみ）。

### ラン終了とやり直し

- **ランには終点がある。** `daysSurvived` が **`run.dayGoal`（既定5・設定欄で変更可）** に達したらランを終了し、リザルトを出す（**到達後は続行させない**）。**まず5営業日を面白くできるかを見る**（20は長すぎた。物足りなければ後で増やす）。設定で変更できる。
- **家賃を払えなかった場合も従来どおり終了する。** リザルトは**到達で終了したのか（完走）／力尽きたのか**を見出しの文言と見た目で区別する。
- **リザルト画面（2026-08-11 追記）**：1画面・縦スクロール（遷移を分けない）。上から次の順で並べる。
  1. **見出し** — 完走（`dayGoal`到達）か途中終了かで文言・見た目を変える。
  2. **最高得点のバーガー** — 実際の積み方を再現した画像＋レシピ（下から順のカード名一覧）＋そのスピンの得点。
  3. **最終デッキ** — 軸ごとの内訳と枚数。
  4. **営業日ごとの売上グラフ**（最後・**対数目盛り**）。78→644→…→41兆のような幅があるため線形では読めない。
  - 出さないもの：総スピン数・主軸。
  - **得点表示は指数表記（4.4e20 等）を使わず、万・億・兆・京・垓…の単位で丸めて読める形にする**（`fmtJP`）。
- **「もう一度（最初から）」でランをやり直す**。所持金・営業日・スピン数・購入回数・デッキ（初期構成に復元）をすべて初期化する。**メタ進行は残さない**（この試作の範囲外）。

### 未決（次の指示以降）

家賃の金額と上がり方・ランの長さ・店の価格は、この測定を通じて詰める。ラン終了後に何を残すか（メタ進行の中身）、カードの複製、常駐の店員は未実装（KENTO.md 参照）。

## 11. データ構造（カードとアーティファクト）（2026-08-12 記録）

**現状の実装を読んで記録したもの。** カードは `CONFIG.items`（カード名をキーにしたオブジェクト）、アーティファクトは `CONFIG.artifacts` に持つ。数値・効果はすべて調整欄から変更できる（第6節）。

### カード（`CONFIG.items` の各エントリ）

| 項目 | 内容 | 現在使われている値 |
| --- | --- | --- |
| （キー） | カード名（日本語） | 「パティ」「ぎょうざ」など |
| `pile` | どの山に属するか | `ingredient` / `sauce` / `bun` |
| `type` | 系統（ソースの効き先） | `meat` / `dairy` / `veg` / `sauce` / `bun` |
| `cuisine` | 軸（デッキの方向づけ） | `none` / `wa` / `chuka` / `kaisen` / `kokyu` / `agemono` |
| `base` | 基礎点 | 数値 |
| `count` | 所持枚数。**非復元抽選により「1バーガーで引ける回数」として機能する**（第2節） | 数値（初期デッキは1以上・非初期は0） |
| `tier` | 発動時の演出の固定等級（0=小/1=中/2=大）。`null`＝自動判定 | 現在は全て `null` |
| `rar` | レアリティ | `nami` / `jou` / `tokujou` / `kiwami`（並／上／特上／極）。**Wave7 Step1 で96枚に本割りを入れた**（極8・特上23・上39・並26。KENTO「96枚のレアリティ割り振り案」） |
| `weight` | **未使用（Wave7 Step0 で撤回）。** 当時の設計（重いほど下に積む）の記録として値は残しているが、コードのどこからも読まれない。並びは抽選で決まる（第2節） | 1〜200（読まれない） |
| `bunOnly` | **バンズの入れ物（Wave7 Step4 で追加）。** 描画と評価が読めるように `CONFIG.buns` の30種を `items` にも登録している印。`true` のものはカードではないので `catalogNames()`・レアリティの枚数・3択・店から外れる | `true`（バンズ30種のみ） |
| `effect` | 効果欄。`{ id, kind?, ...パラメータ }` | 下記 |
| `ph` | 専用の絵をまだ持たない仮カードのフラグ（描画は汎用タイル＋名前ラベルで代用） | `true` の軸カード（初期無印は省略） |

- **`effect.id`**：効果の識別子。コード側の `applyCardEffects` の分岐（`switch(e.id)`）と対応する。効果を持たないカードは `id:null`（Wave1 で全カード null にし、Wave2 で15枚に既存エンジンの id を設定した。残りは Wave3〜4 で有効化予定。対象表は LOG.md「Wave 2」）。
- **`effect.kind`**：効果の型（KENTO.md「カードの効果の型」）は**分類のための文書上の欄で、発動処理は使わない**（`applyCardEffects` は `id` で分岐する）。**現状 Wave2 で設定した15枚を含め、どのカードにも `kind` は付けていない**（`id` とパラメータだけ）。型の整理は今後の作業。
- **効果パラメータ**：`per` / `add` / `mult` / `need` / `max` / `ref` / `targ` / `targetCui` など、効果ごとに異なる欄を持つ。表示文（プレイヤーが読む説明）は `effectText(name)` が現在の数値から生成する（第5節の必須要件）。

### アーティファクト（`CONFIG.artifacts` の各エントリ）

盤面に乗らず常時働く（第3層）。店で購入し、所持上限まで持てる。

| 項目 | 内容 | 例 |
| --- | --- | --- |
| （キー） | アーティファクト名 | 「厨房の火力」「深夜営業」など |
| `id` | 効果の識別子（コード側と対応）。適用箇所は得点（`applyArtifacts`）・抽選・経営（家賃／店） | `meatBase` / `axisWa` / `nightshift` など |
| `rar` | レアリティ | `nami` / `jou` / `tokujou` / `kiwami` |
| （効果パラメータ） | 効果ごとに異なる欄 | `add` / `mult` / `need` / `extra` / `ratio` / `weight` など |

### `weight`（重さ）— Wave7 Step0 で撤回

- **`weight` はもう読まれない。** Wave1 で全カードに入れた「重さ」は、盤面の整列に使う値だった。Wave7 Step0 で並びを抽選にしたため、`cardWeight()` と整列処理、起動時セルフチェックの「重さ未設定の検出」、デッキ一覧の「重さ順」と重さの表示、3択・店の重さの表示を**すべて撤去した**。
- **フィールド自体は残してある。** 96枚ぶんの値は当時の設計の記録であり、コメントで未使用と明記している。調整欄に行は無い。
- **`weightOf()` とは別物である。** `weightOf()` は「抽選の当たりやすさ（レアリティ重み）」で、Wave7 Step1〜2 で `axisRates()` / `rarityRatesFor()` / `drawOffer()` に作り替えた（第15節）。撤回したのは盤面の整列に使う `weight` だけ。

### バンズ（`CONFIG.buns` の各エントリ）（Wave7 Step4）

**デッキとは別領域。** 31種（つなぎのゴマバンズ＋KENTO §6 の30種）。

| 項目 | 内容 | 例 |
| --- | --- | --- |
| （キー） | バンズ名（日本語） | 「ソースのバンズ」「総出のバンズ」など |
| `rar` | **バンズ専用のレアリティ**（カードの4段とは別の3段） | `common` / `rare` / `kyoku`（コモン／レア／極） |
| `base` | 基礎点。**全種0**（フィールドは残す＝後から上げられる） | `0` |
| `mult` | そのバンズが素で持つ、バーガー全体への倍率 | `1.0`〜`4.0` |
| `effect` | 効果欄。`{ id, per?, ref?, min?, step?, uses?, pow?, half? }` | `{ id:"bunPerSauce", per:1.25 }` |

- 効果文は `bunText(name)` が現在の数値から生成する（第5節の必須要件）。判定は `bunOneEffect()`、合成は `applyBunLayer()`。
- 個体ごとの状態：`g`（毎スピンの伸び・#19 仕込みのバンズ）／`uses`（使った回数・#21 使い切りのバンズ）。

### アーティファクト（`CONFIG.artifacts`）（Wave7 Step5 で45種に作り替え）

| 項目 | 内容 | 例 |
| --- | --- | --- |
| （キー） | アーティファクト名 | 「暖簾」「売り子」「前借り」など |
| `id` | 効果の識別子。適用箇所は**供給（軸の重み・確定枠）・選択の回数・枠・店の構造・ラン構造**。**得点には触らない** | `axisWeight` / `shopCards` / `rentSkip` など |
| `rar` | レアリティ（カードと同じ4段） | `nami` / `jou` / `tokujou` / `kiwami` |
| `pick` | `true` なら「軸を選ぶ型」。買った直後に共通の軸選択画面が出る | `true`（暖簾・老舗の暖簾・常連の好み・一点張り・予約票・下ごしらえ） |
| （効果パラメータ） | 効果ごとに異なる欄 | `mul` / `need` / `n` / `per` / `times` / `step` / `ing` / `slots` / `onBoard` / `shopCards` / `extra` / `ratio` など |

- **`applyArtifacts` から得点の分岐は消えた。** 廃止した4個（厨房の火力・製氷機・衛生管理・常連客〈旧〉）と、軸の効果を×1.3する5個は撤去した。
- **〈使い切り〉は `ARTIFACT_ONCE` に id を列挙する**（7種）。使うと `consumeArtifact()` で所持枠から消える。

## 12. 育ち（個体管理）（Wave3・2026-08-12）

**「育ち」＝ラン中、カードの個体に永続で積み上がる整数カウンタ。** 海鮮軸の資源。バンズは対象外（`weight:0`・育ちを持たない）。

- **得点式では「第2項」として独立している**（第4節）。**育ち×15**（`GROW_PER`・**15 は仮値・確定ではない**）。
- **出汁・醤油の修飾は第2項にだけ効く。** 出汁＝盤面で最も育った値を上下2枚の育ちとして計算／醤油＝上下2枚の「育ち×15」を2倍。どちらもそのスピンのみで、育ち値そのものは変えない（魚醤・いか・いくらは**生の育ち**を読む）。
- **第1項（基礎点）を読む効果は育ちを読まない**（蟹カマ・飴細工・金箔・フォアグラ・あわび）。育ちは海鮮の縛りであり、他軸が取り込めないようにするため（第4節・KENTO）。

### 個体管理（データ構造）

- **デッキは「名前×count」ではなく「個体の集合」`deck`**。各個体は `{ id, name, g }`（`g`＝育ち）。**同名でも別々の育ちを持つ。**
- `CONFIG.items[name].count` は**個体数のキャッシュ**（`syncCounts()` で常に同期）。枚数・軸内訳・段数上限などの集計はこのキャッシュを読む。カードの定義（base/weight/effect 等）は従来どおり `CONFIG.items[name]`。
- **引き・整列・得点・取り除き・廃棄はすべて個体単位。** 購入＝育ち0の新しい個体を1つ追加。**取り除き・廃棄された個体の育ちは消える（復活しない）。買い直しは育ち0から。**
- **【追記・Wave 12】消えたカードは原因つきで画面に出す。** スピン中に消えたぶんは完成の内訳欄（`#firedList`）に、営業日をまたぐときに消えたぶんは店の見出しの下に、「◯◯があわびに食べられた」の形で1行ずつ出す。**店の「取り除く」だけは出さない**（プレイヤー自身がその場で選んだ操作で、棚の表示がすぐ更新されるため）。
- **取り除きUI・デッキ一覧は個体単位**で、同名でも育ち値が見えて選べる（例：ほたて(育ち3)／ほたて(育ち7)）。
- **育ち値はカード面に常時表示**（盤面・デッキ一覧・取り除き画面）。育ち0は表示しない。

### スピン内の処理順序（固定）

1. **スピン開始**：うに系の毎スピン成長と判定（全個体+3→その後20%で育ち半減＝切り捨て。判定はシードRNG）。
2. **引き**：引かれた個体への成長（ほたて＝+1・このスピンの得点に含む）。
3. **整列**（重さ順・第2節「盤面の整列」のまま）。
4. **盤面成長**：隣接判定に基づく成長（白身魚＝上下が海鮮の組ごとに自分+2・永続・このスピンの得点に即時反映）。
5. **得点計算**：基礎点 = base + 育ち×15（醤油＝上下の育ち×15を2倍／出汁＝盤面最大育ちを上下2枚の育ちとして計算・そのスピンのみ・育ち値自体は変えない）→ 効果適用（魚醤・いか・いくらは**生の育ち**を読む）。
6. **スピン終了**：引かれなかった個体への成長（昆布＝盤面に出なかった個体+1）。

- **さけの加速**は、育ちが n 増える**すべての成長イベントに対するフック**として働く（`grow()`：+n のたびに さらに +n＝実質2倍。半減など減少には効かない）。
- **新しい乱数はすべてシードRNG（mulberry32・`grand()`）経由**（うにの20%判定など）。`Math.random` は使わない。
- **プレイログ**：各スピンの記録に盤面カードの育ち（`growth`・下から順）を含める。最高得点バーガーにも育ちを保持。

## 13. 永続欄（pb / pm / uses / charge）とイベントフック（Wave4・2026-08-12）

個体（第12節）は育ち `g` のほかに次の永続欄を持つ。**すべて個体単位**で、同名でも別々に伸びる。購入した個体は全欄0から始まり、**廃棄・取り除きで消えた個体の値は復活しない**。

| 欄 | 意味 | 得点への効き方 |
| --- | --- | --- |
| `g` | 育ち（海鮮の資源・第12節） | 第2項（×15） |
| `pb` | 永続の基礎点上乗せ（整数） | 第1項に加算 |
| `pm` | 永続の倍率上乗せ | 盤面に出たとき `×(1+pm)` |
| `uses` | 発動・使用の回数 | 直接は効かない（上限で廃棄などの条件に使う） |
| `charge` | 未消費の付与権（キムチ） | 直接は効かない（盤面で使って効果に変わる） |

- **`g` と `pb` は別物。** 育ちは海鮮の縛り。営業日・発動回数で伸びるカードは `pb` / `pm` を使う。
- **値はカード面に常時表示する**（盤面・デッキ一覧・取り除き画面。0 は出さない）。プレイヤーが選ぶ時点で読めること（第5節の必須要件）。
- **永続の倍率には上限を置く**（`effect.pmMax`・ラーメン／継ぎ足しのタレは 1.5＝×2.5・**仮値**）。調整欄から変更できる。`maxMul`（倍率そのものの上限・ソース焼きそば）とは別物。

### 倍率が個体ごとに育つソース（Wave9・2026-08-13）

ソースの中に、**盤面の外で起きたこと（3択で取る・盤面に出ない・カードが失われる）を餌にして、倍率そのものが個体ごとに育つ**型がある（根拠は KENTO「打点の物差しとソースの設計」§4）。

| 欄 | 意味 |
| --- | --- |
| `effect.pm0` | **その効果を持つカードの `pm` の初期値。** 個体を作る `addInstance()` が読む。成長ソース5枚は 0.5（＝×1.5 から始まる） |
| `effect.per` | 条件を1回満たしたときに `pm` に足す量 |
| `effect.pmMax` | `pm` の上限（成長ソース5枚は 5.0＝×6） |

- **`pm0` は個体を作る1か所（`addInstance()`）でだけ読む。** 3択・店・初期デッキ・調整欄の「＋」・計測口はすべてここを通るので、経路ごとの分岐は無い。`pm` を明示的に渡した呼び出しはその値が優先される（`!= null` で判定）。
- **`pm0` を持たないカードは従来どおり `pm:0`（＝×1.0）から始まる。**
- **育った倍率は永続で、個体ごとに違う。** 同名2枚が別々に育つ。**廃棄・取り除きで個体が消えれば、その育ちも消える**（買い直しは `pm0` から）。
- **上限に達したらそれ以上育たない**（`Math.min(pmMax, …)`）。
- **現在の倍率はバッジで読める**（盤面の `pm-badge`＝×2.1／デッキ一覧・取り除き画面の `pm-inline`）。**効果文には条件・1回あたり・上限だけを書き、現在値は書かない**（二重に出さない）。
- **餌の判定位置**：3択で取る＝`chooseOffer`（取る**前**のデッキの主軸で判定）／盤面に出なかった＝スピン終了（昆布と同じ位置。組めない回は走らない）／隣の育ちを吸う・並と接する＝盤面成長（そのスピンの得点に反映）／特上以上が失われた＝廃棄フックと**店の取り除きの呼び出し側**（下記）。
- **店の取り除きは「廃棄」ではない（D3）が、成長の餌としては数える。** D3 の本体（天かす・白トリュフ）は従来どおりフックを走らせず、成長ソースのぶんだけ呼び出し側から明示的に数える。理由：このカードの狙いが「取り除きに積極的な理由をつける」ことなので、主要な削除経路である店を外すと狙いの半分が消えるため。
- **「加算のバンズ」（極・#28）は、育った `pm` が出した倍率も加算に変換する。** `pm` のヒットはソースの位置に紐づく乗算ヒットとして積まれるため、変換の対象に入る。**育ちきった ×6 は +500 になる。これは仕様として許容する。** 成長ソースを入れたことで、このバンズに初めて明確な仕事ができた。

### イベントフックと処理順序

スピン内の順序は第12節のとおり（①スピン開始 →②引き →③整列 →④盤面 →⑤得点 →⑥スピン終了）。各フックの呼び出し位置：

| フック | 呼ぶ場所 | 使うカード |
| --- | --- | --- |
| `onSpinStart()` | `buildPlan()` 冒頭（①・**盤面が組める回だけ**） | ラーメン・継ぎ足しのタレ |
| `onDraw(insts)` | 引きの直後（②） | 北京ダック |
| `onBoard(inst, stack)` | 整列後・得点前（④） | 油揚げ・キムチ・かに・あわび・カレーソース・引き系の予約 |
| `onComplete(spinTiers)` | `collect()`（回収時） | メンチカツ・すぶた・カレーソースの自壊 |
| `onDay()` | 家賃の支払い成功時（`daysSurvived++` の直後） | 熟成肉・しらす・かまぼこ・キムチ・マッシュルーム |
| `onBuyCard(inst)` | `buyCard()` | 搾菜 |
| `onDiscardSelf/onAnyDiscard` | `discardInstance()` | 天かす／白トリュフ |

- **空引きの回は①②④を起こさない。** 引く前に `canBuild()`（乱数を使わない判定）で「組めるか」を見てから成長させる。これがないと、山が空の回に `onSpinStart` が毎タップ発火して永続倍率が伸びてしまう。
- **廃棄（`discardInstance`）と店の取り除きは別物。** 廃棄＝効果で消えること（キャビア・かつおぶし・かに・あわび・カレーソース）はフックを走らせる。**店の取り除き（有料）はフックを走らせない**（数えると「金を払って回数を買う」経路になるため）。
- **効果解決は2段。** `resolveCard(i, ctx)` が hit を返し、`applyHits` が第3項と乗算へ反映する。ドライバの順序は固定：**①軸の読み替え →②のりの成立数 →③賭けの必勝 →④発動回数 →⑤解決と反映 →⑥pm →⑦味醂／フライドポテト →⑧アーティファクト →⑨メロンパン**（コンボ数ボーナスは `evaluate` 側）。軸の判定はすべて `ctx.cuiAt(i)` を経由し、**読み替えは一度だけ確定させる（再帰させない）**。
- **引く枚数の増減は盤面トリガで次スピン1回だけ**（貴腐ワイン・白味噌・ツナ・中華まん）。対象は具材枠のみ・下限1。実効値は段数表示に出す。
- **廃棄はその山の最後の1枚を持っていかない**（Wave5）。店の取り除きと同じ条件。これが無いとキャビア等がバンズを食い切って「組めないのにタップしても何も起きない」状態で止まる。それでも組めなくなったら、黙って止めずにリザルト（閉店）へ進める（調整パネルを開いている間はメッセージだけ）。

### 第1項（基礎点）の書き換え順序

第1項を書き換える効果は `baseArray` の中で**この順で一度だけ**通す（再帰させない）。**軸の判定はすべて読み替え表（`cuiMap`）を経由する**ので、読み替え表は `baseArray` より前に確定させる。

| 順 | カード | すること |
| --- | --- | --- |
| ① | 福神漬け | 上下2枚が互いに違う軸なら、その2枚に +55（加算） |
| ② | あわび | 吸われた下1枚を 0 にする（そのスピンのみ） |
| ③ | 飴細工 | 上下2枚の第1項を入れ替える（自分は0） |
| ④ | かつおぶし | 自分を 0 にする（隣接2枚を2回発動させる代わり） |
| ⑤ | 蟹カマ | 最も低い第1項を、最も高い第1項と同じ値にする |

**加算が先・底上げが後**。こうすると福神漬けの +55 が蟹カマの「最高」に反映され、フォアグラ・金箔も書き換え後の値を読む。同じ効果が複数枚あるときは下の段（index の小さい方）から順に適用する。**どれも育ちを読まない**（第4節）。

### 起動時セルフチェック（12項目・Wave5 で追加／Wave7 で拡張）

`console.warn` にだけ出す（プレイヤーには見せない）。**常に0件を保つ。** 新しく作った仕組みにも同じ形の点検を足す。

| # | 見るもの | 見つかると困ること |
| --- | --- | --- |
| ① | 効果があるのに効果文が無い | 3択・店で「効果なし（暫定）」と出る（第5節違反） |
| ② | 効果文があるのにどのカードも使っていない | 死んだコード |
| ③ | 効果があるのに `resolveCard` が処理していない | 静かに何も起きない |
| ⑤ | 知らない `cuisine` | 軸の集計から漏れる |
| ⑥ | 知らない `rar` ／ レアリティの枚数が KENTO と違う（極8・特上23・上39・並26） | 抽選の比率が設計とずれる |
| ⑦ | 出現率の3行の合計が1でない／極が上限を超えている | 抽選が設計どおりにならない |
| ⑧ | 「並」が1枚も無い軸がある | その軸を引くたびフォールバックが起きる（設計の穴） |
| ⑨ | 軸の出現率の合計が1でない／下限を割る／上限を超える | 傾斜の式が壊れている |
| ⑩ | 店の無印固定枠が並ぶ枚数を超える／1営業日の回数が未設定 | 店が組めない |
| ⑪ | バンズ：効果文の欠落／知らないレアリティ／基礎点が0でない／30種の内訳が違う（コモン12・レア15・極3） | バンズの層が設計とずれる |
| ⑫ | アーティファクト：45種でない／効果文の欠落／`id` の重複／〈使い切り〉が7種でない／廃止した型の残留／所持枠が3でない | 第3層が設計とずれる |

**④（バンズ以外で `weight` が無い／0）は Wave7 Step0 で撤去した**（並びが抽選になり `weight` が読まれなくなったため）。番号は詰めずに欠番のまま残している。

①②⑤〜⑧はカードだけを数える（`catalogNames()` が `bunOnly`＝バンズの入れ物を外す）。バンズは⑪、アーティファクトは⑫で別に点検する。


---

## 14. バンズの層（4枠2枚抽選）（Wave7 Step4・2026-08-13）

**バンズはデッキとは別の領域である。** 得点の作り方そのものに条件と報酬を課す層で、営業日ごとに強制的に入れ替わる（根拠は KENTO「バンズの設計」）。

| 項目 | 決定 | 調整欄 |
| --- | --- | --- |
| 所持枠 | 4枚 | `params.buns.slots` |
| 盤面に出る枚数 | 2枚（4枚から非復元で抽選） | `params.buns.onBoard` |
| 上下 | どちらが上に来るかも抽選 | — |
| 入手 | **営業日の最後のスピンの報酬をバンズ固定にする。見送れない** | — |
| 押し出し | 入手のたびに1枚捨てる。**捨てるものはプレイヤーが選ぶ** | — |
| 基礎点 | 0（フィールドは残す） | `CONFIG.buns[名].base` |
| 倍率 | バーガー全体に掛かる（第4節「バンズの層」） | `CONFIG.buns[名].mult` |
| レアリティ | バンズ専用の3段（コモン／レア／極）・専用の抽選 | `params.buns.weights` |

- **抽選は `buildPlan()`（計算層）で確定する。** 同一シードなら盤面に出る2枚と上下が完全に再現する。
- **3択の代わりに出るので、選択の回数は変わらない。** 営業日の切れ目に「バンズ入替 → 家賃 → 店」が並び、区切りが決算と再編成になる。
- **ラン開始時の4枠は、つなぎのゴマバンズ4枚で埋める。** 抽選で埋めない理由：デッキが全部無印の白紙から始まるのと同じで、ルールも取ったものだけが作る。ゴマバンズは押し出しで自然に消えていく。
- **バンズは3択にも店にも並ばない**（`offerPool()` が `pile:"bun"` を外す）。買えず、狙えず、営業日ごとに必ず変わる。
- **所持している4枚の効果は常に読める**（デッキ一覧の下に4枠を出す）。4枚の噛み合わせを毎営業日考えるのが遊びの中心なので、いつでも見渡せることを優先した。
- **並びを要求するルールは残してある**（#5 上下対称・#6 昇順・#10 同軸2枚並び・#11 同系統2枚並び）。並びが抽選になったので成立は確率になるが、枚数を増やせば期待値が上がるため、軸を厚くする圧として働く。
- **もう1枚に干渉する2種**（#23 相棒＝もう1枚の倍率を2倍／#25 写し＝もう1枚の効果を2回適用）は、2枚抽選という仕組みを直接使う。
- **極3種**は得点式そのものに触る：#28 加算のバンズ（ソースの倍率を加算に変換）／#29 見立てのバンズ（不発だったカードを半分の効き方で発動扱い）／#30 総出のバンズ（所持枠すべてが働く・各倍率は0.6乗）。
- **実装しないもの**：KENTO の保留3種（得点=金に依存する2種・家賃に依存する1種）。ただしこれらは**30種の連番の外にある別表**なので、**30種はすべて実装している**。

## 15. 抽選（軸 → レアリティ → カード）（Wave7 Step1-2 → Wave8 で更新）

3択と店の品揃えは、**3段の抽選**で1枚ずつ引く。狙いは、レアリティの出る比率をそのレアリティの枚数から切り離すこと（極が8枚でも23枚でも、極が出る確率は表のとおりになる）。

### ① 軸を引く

デッキの軸構成から5軸（`wa` / `chuka` / `kaisen` / `kokyu` / `agemono`）の出現率を出す。**無印は3択の抽選から外す**（店専用・第16節）。

```
share_i = c_i / (C + m)                  C=軸カードの総枚数、m=「まだ決まっていないぶん」の重み
  ↑ share の合計は C/(C+m) で1未満になる。余り（m/(C+m)）は5軸すべてに均等に配る。
    これで share の合計は必ず1になり、軸カードが0枚のときは各 1/5 になる。
q_i     = (1 - k)/5 + k * share_i         k=1 で share にそのまま比例、k=0 で完全均等
p_i     = floor + (1 - 5*floor) * q_i     下限を全軸に確保し、残りを q で配る
```

`k`=1・`floor`=0.10 のとき、主軸の出現率は次の1本の式にまとまる。

```
p_main = floor + (1 - 5*floor) * (5c + m) / (5(c + m))        c=その軸の枚数
```

- `m`（仮値 **8**）・`k`（仮値 **1.0**）・`floor`（仮値 **10%**）はすべて調整欄から変更でき、いまの出現率が読み取り専用で表示される。
- **`m` は Wave8 Step0 で入れた。** Wave7 の式（分母＝軸カードの合計枚数）では**軸カードを1枚取った瞬間に `share`=1 になり、それ以上寄らなかった**（実測：揚げ物1枚でも9枚でも主軸44%）。分母に `m` を足すと枚数に応じて寄っていく。**`m` を小さくすると速く寄る。**
- 主軸の出現率（`m`=8・`k`=1.0・`floor`=10%）：**0枚 20% → 1枚 24.4% → 5枚 35.4% → 10枚 42.2% → 20枚 48.6%。**
  - **0枚から1枚へ移る時点から単調に増える。** 余りを「まだ取っていない軸」だけに配る案も検討したが、それだと0枚→1枚でその軸の率が下がってしまう（取った瞬間に余りの配分から外れるため）。**1枚目こそ軸を決める瞬間なので、そこで逆に働くのは傾斜の目的に反する。** 5軸に均等に配れば単調増加が保証される。
  - `m` を変えたときの主軸の出現率（`floor`=10%）：

    | 枚数 | m=4 | m=8 | m=16 |
    | --- | --- | --- | --- |
    | 0 | 20.0% | 20.0% | 20.0% |
    | 1 | 28.0% | 24.4% | 22.4% |
    | 5 | 42.2% | 35.4% | 29.5% |
    | 10 | 48.6% | 42.2% | 35.4% |
    | 20 | 53.3% | 48.6% | 42.2% |
- この式で「合計1」「どの軸も `floor` 以上」「主軸は最大 `1-4*floor`（下限10%なら60%）」「軸カードが0枚なら各20%」が同時に満たされる。
- **主軸が同数のときは `AXES` の固定順**（和→中華→海鮮→高級→揚げ物）で先に来る軸を主軸とする（乱数を使わない＝再現性のため）。なお同数の軸は距離も0になるので、どちらを主軸にしてもレアリティ表は同じになる。
- アーティファクトは比例のぶん（`q`）に掛けて正規化するので、下限と上限を壊さない（暖簾・老舗の暖簾・常連客）。

### ② レアリティを引く（進行 → 軸の距離の2段・Wave8 Step1）

**Wave7 は「主軸からの距離」だけで表を選んでいた。** そのためラン開始時はデッキに軸カードが0枚で5軸すべてが「遠い軸」扱いになり、**1営業日目からいきなり 並25・上30・特上35・極10 の表が適用されていた**（実プレイのログでスピン2でテリヤキソース＝極が提示された）。**進行の軸を足して2段にした。**

**手順**：①進行度から基準の4値を引く → ②その軸の距離に応じた倍率を掛ける → ③合計1に正規化 → ④極を上限（10%）でクランプ → ⑤そのレアリティのカードから1枚引く。

**①進行の基準**（`rarity.progress`）。進行度 = **現在の営業日 / `dayGoal`** で正規化するので、`dayGoal` が 5 でも 10 でも同じ曲線になる。表の点の間は線形補間し、最初の点より前は最初の行で止める。

| 進行度 | 並 | 上 | 特上 | 極 |
| --- | --- | --- | --- | --- |
| 0.2 | 78% | 20% | 2% | 0% |
| 0.4 | 65% | 27% | 7% | 1% |
| 0.6 | 50% | 33% | 15% | 2% |
| 0.8 | 38% | 36% | 23% | 3% |
| 1.0 | 30% | 37% | 30% | 3% |

**極の基準は最大3%。** 進行がいくら進んでも基準では3%を超えない。

**②軸の距離による倍率**（`rarity.distMul`）。**加算ではなく倍率**にしてある。加算だと「1営業日目の極は基準0%なのに、遠い軸なら +7 されて7%になる」という破綻が起きる。倍率なら 0%×3.3=0% のまま。進行と距離が掛け算になるので、**「序盤は何をしても極が出ない、終盤に遠い軸なら極が出る」**という形になる。

| | 並 | 上 | 特上 | 極 |
| --- | --- | --- | --- | --- |
| 主軸（距離0） | ×1.3 | ×1.0 | ×0.6 | ×0.5 |
| 遠い軸（距離1） | ×0.7 | ×1.0 | ×1.8 | ×3.3 |

中間は距離で線形補間する。**距離の定義**は主軸＝0／デッキに0枚＝1（最遠）／その間はデッキ内枚数で線形補間（`1 - c_i / c_main`）。

- **【追記・Wave 12】上の8つの既定値は、すべて ×1.0 にした。** 軸の距離でレアリティを動かすのをやめ、**どの軸でも進行度だけでレアリティが決まる**（②の段は掛け算としては残るが、既定では何もしない）。**仕組みは残してあるので、調整欄「レアリティ：軸の距離による倍率」から8つとも編集でき、上の表の値に戻せる。**

- **無印は軸ではないので距離が定義できない。中間（`rarity.noneDist` 既定 0.5）として扱う。** 店の無印固定枠にも進行の表がそのまま効く。
- 進行の表・倍率の表・`noneDist`・極の上限はすべて調整欄から編集できる。

### ③ そのレアリティの中から1枚（均等）

- **候補が1枚も無いレアリティを引いたら、隣のレアリティに落とす**（極→特上→上→並の順。それでも無ければ上へ）。**落ちたことは記録する**（`RUN.rarityFallbacks()` とプレイログの `rarFallbacks`）。無印に極が無いので、無印を引くときはここを通る。
- **デッキから盤面に引くのは個体ごとに均等で、レアリティ重みは乗らない**（第2節）。レアリティは「入手のしやすさ」であって「引きやすさ」ではない。

## 16. 店の作り（Wave7 Step3・2026-08-13）

| 項目 | 決定 | 調整欄 |
| --- | --- | --- |
| 並ぶカード | 6枚 | `params.shop.cardsShown` |
| うち無印固定枠 | 2枠（残り4枠は5軸から傾斜つき） | `params.shop.noneSlots` |
| リロール | 1営業日1回（**回数制。値上がりではない**） | `params.shop.dayLimits.reroll` |
| 具材枠の購入 | 1営業日1つまで | `params.shop.dayLimits.ing` |
| ソース枠の購入 | 1営業日1つまで（上限3・固定価格は Wave6 のまま） | `params.shop.dayLimits.sau` |
| 削除 | 1営業日1回まで | `params.shop.dayLimits.remove` |
| カードの購入枚数 | **上限なし**（並んだ6枚は全部買える） | — |

- **無印は3択に出さず店専用にした。** 無印13枚は全部「多様性」に振り切っているので、軸に寄せたデッキに混ざると死ぬ。店専用にすれば「寄せない遊び」を選んで買う裏路線として成立する。**2枠を固定にするのは、出てこないものは強さを判断できないため。**
- **買えなくなったものはボタンを消さず、無効表示にして理由を出す**（「今日はもう買えません」「ソース枠は3枚まで」「所持金が足りません」）。買えないことが見えるほうがよい。
- **リロールと削除の値上がりを止めた**（常に基準価格）。回数で切ったうえに値上がりも掛かると、営業日をまたぐごとに実質使えなくなる。削除は ¥75 の固定コストになる。
- **カードの購入枚数に上限を置かない理由**：枠は買えば必ず得だが、カードは買うと命中率が下がる。買うこと自体に代償が組み込まれているので、外付けの規則は要らない。
- 回数は営業日が変わると回復する（`onDay()` の `resetShopDay()`）。

## 17. アーティファクトの層（3枠）（Wave7 Step5・2026-08-13）

| 項目 | 決定 |
| --- | --- |
| 所持枠 | **3枚**（`params.artifacts.maxOwned`） |
| 入れ替え | **自由。回数制限なし。型による例外もなし** |
| 入れ替えで捨てたとき | **金は戻らない**（仮決定） |
| 得点への干渉 | **しない。** 数値を足したり倍率を掛けたりしない |

- **変えるのは「できること」＝供給・選択の回数・枠・店の構造・ラン構造。** ランが長いと平らな数値は必ず腐るため（M1：肉の基礎点+15 に対し、同額の熟成肉が `pb` 216 まで伸びていた）。
- **バンズとの棲み分け**：バンズが得点の作り方を決め（4枠・強制入替・**振り回されるもの**）、アーティファクトが選択の仕方を決める（3枠・任意で入れ替え・**自分で組むもの**）。
- **3枠にした理由**：型が4種類あるので4枠以上あると全部揃えられてしまい判断が消える。3枠なら**何を諦めるかが個性になる**。
- **「確定枠」は1つの仕組みに統一した**（`guaranteedSlots(kind)` → `guaranteedPicks()`）。3択も店の並びも同じ関数を通る。指定の条件を満たすカードで必ず埋まる枠で、**残りは通常の抽選**（報酬が全部同じ軸になると単調になるため、1枠だけ保証する形にした）。
- **軸を選ぶ型は共通の軸選択画面を1つ使う**（`showAxisPick()`）。買った直後に出て、所持中のチップのタップで選び直せる。未選択のあいだはデッキの主軸を使う。
- **〈使い切り〉7種**（予約票・下ごしらえ・値札の張り替え・夜の仕込み・朝市・前借り・見習いの独立）は使うと枠から消える。3枠しかないので、**枠が空くこと自体が価値になる**。
- **実装しないもの**：KENTO の保留1種（貯金箱）。これも**45種の連番の外にある別表**なので、**45種はすべて実装している**。
- **旧15個のうち4個（仕入れ帳・メニュー表・レジ係・冷蔵庫）は45種に無い。** §6 の同等物（暖簾＝軸の重み／売り子・立て看板＝店の枚数／廃棄業者＝削除が無料）に一本化された結果として消えた。**レジ係（家賃の一部が戻る）だけは45種に同等物が無い。**

---

## 18. カードの構成（Wave10・2026-08-14）

**カードは101枚。** 内訳は **具材72枚・ソース29枚**。バンズ30種は別領域（第14節）で、この数には入らない。設計の根拠は KENTO「具材72枚の確定（2026-08-14・決定）」「ソース29枚の確定（2026-08-14・決定）」。**数値はすべて仮で、調整欄から動かせる。**

| 山 | 枚数 | 内訳 |
| --- | --- | --- |
| 具材 | **72** | 6軸 × 12枚（並3・上4・特上4・極1／無印だけ 並5・上4・特上2・極1） |
| ソース | **29** | 5軸 × 4枚（各帯1枚）＋ 無印9枚（並1・上3・特上4・極1） |
| バンズ | 30 | 第14節。**この波では触っていない**（例外はゴマバンズの基礎点だけ） |

- **ソースの基礎点は全部0。** ソースは乗算の層で、具材＝基礎点と加算、バンズ＝ルール、という三層を分ける（KENTO「打点の物差しとソースの設計」§1-6）。
- **効果を持たないバンズだけが基礎点を持つ**（ゴマバンズ20・他29種は0）。**これがバンズにおける「並」の定義。** 起動時セルフチェック⑪もこの形で点検する。
- **ソースの並と上は「そのカードだけ」に倍率が掛かる。** 並＝盤面の同軸のカードを ×1.5／上＝上下に接する同軸のカードを ×3。**対象の広さと倍率のトレードオフ**になっている。得点式は3項×乗算でカード単位の倍率を持てないので、**「対象カードの第1項の (N−1) 倍を第3項に足す」**形で表している。
- **「育ち」は `pb`（第1項の永続加算）に一本化した。** 効果文の表記は「**+N点（永続）**」で統一する。「育ち+1」という表記は使わない。**第2項（`g` × `GROW_PER`）は供給側が無くなったので常に0**（式は3項のまま残してある）。
- **初期デッキは6枚**（パティ60／チーズ50／きゅうり40／ケチャップ0／ゴマバンズ20×2）。**基礎点に差を付けてある**ので、序盤に「どれから捨てるか」の判断が生まれる。
- **初期デッキのカードは店に並ばない**（パティ・チーズ・きゅうり・ケチャップ）。絶対に買わないカードが店の枠を潰すのはストレスになるため。3択には元から出ない（`AXES` に `"none"` が無い）。
